[project]
name = "jira-voc-nexus"
version = "0.1.0"
description = "Local dry-run Jira VOC triage proposal scaffold"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
rag-platform = ["opensearch-py>=2.4"]
rag-pg = ["psycopg[binary]>=3.1"]
rag-reranker = ["torch>=2.2", "transformers>=4.44"]

[tool.unittest]
start-directory = "tests"
